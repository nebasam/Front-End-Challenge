import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles((theme: any) => ({
  mainContainer: { display: "flex", flexWrap: "wrap", flexDirection: "row" },
}));

export { useStyles };
